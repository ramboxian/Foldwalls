# Cloudflare R2 server-side migration credentials. Never expose these values in
# the Sanity Studio browser bundle and never commit a real .env file.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=foldwalls-media
R2_PUBLIC_BASE_URL=https://media.example.com
